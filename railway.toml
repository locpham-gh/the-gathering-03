[build]
builder = "NIXPACKS"
buildCommand = "bun install && cd apps/server && bun run build"

[deploy]
startCommand = "cd apps/server && bun run src/index.ts"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
